import React from 'react';

/**
 * This component has been deprecated.
 * Its functionality (dictation and read-aloud) has been integrated directly 
 * into relevant tool components for a more unified and intuitive UX.
 * Dictation now uses the `useWebSpeech` hook with a microphone button in text areas.
 * Read-aloud is handled by an on-demand "Listen" button.
 */
const FloatingVoiceGuide: React.FC = () => {
    return null;
};

export default FloatingVoiceGuide;
